//iterables 
//in which loop can be applied like string and array

const name = 'selva'
for(let char of name){
    console.log(char)
}

//array like objects
//in which we can apply length property and access via index
//eg string

console.log(name.length)
console.log(name[3])